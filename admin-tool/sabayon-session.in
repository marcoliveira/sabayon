#!/bin/sh

if test -f @PKGDATADIR@/sabayon-session.py ; then
    export PYTHONPATH=@PKGDATADIR@
    exec python @PKGDATADIR@/sabayon-session.py $@
fi
if test -f ./sabayon-session.py ; then
    dir=`pwd`
    dir=`dirname $dir`
    export GLADEDIR=./
    export PYTHONPATH=$dir/lib
    exec python ./sabayon-session.py $@
fi
if test -f admin-tool/sabayon-session.py ; then
    dir=`pwd`
    export GLADEDIR=admin-tool/
    export PYTHONPATH=$dir/lib
    exec python admin-tool/sabayon-session.py $@
fi
